# 2026-09-03

- 将 mema-twin 接入 WorkBuddy：
  - Skill 安装到 `~/.workbuddy/skills/mema-twin/SKILL.md`（复制自项目 `skill/SKILL.md`，内容纯指令无脚本，安全）。
  - MCP 合并进 `~/.workbuddy/mcp.json` 的 `mcpServers.mematwin`（key: `mema-twin`）：command 指向项目 `.venv/bin/mema-twin`，env 中 `MEMA_TWIN_CLIENT_ID=workbuddy`（区别于 zcode 示例）、`MEMA_TWIN_AGENT_ID=mema-twin`、`MEMA_TWIN_WORKSPACE=mema-twin`、`MEMA_TWIN_MEMA_URL=http://127.0.0.1:8000/mcp`。
  - stdio 冒烟测试通过：initialize OK，tools/list 返回单工具 `twin`（动作式：write/get/compile/submit/task_* 等 22 个动作）。
  - 注意：twin 依赖 mema HTTP MCP（127.0.0.1:8000）在线；新 MCP 需在 WorkBuddy 连接器管理页点"信任"后生效。
- 实测 mema-twin 资源占用：idle RSS ≈ 55 MB（无向量模型，纯 mcp+pydantic+httpx），venv 磁盘 117 MB，twin.sqlite3 仅 128 KB。
- 用户级主记忆 `~/.workbuddy/MEMORY.md` 更新：plan-mode（0.4.0）硬规则整节废弃删除，替换为「mema-twin（产出物任务必走）」硬规则——所有产出/更新产出物的工作必须先 `twin(task_start)` 取分身 prompt，交付走 task_submit/task_review，修改意见先 twin.write 沉淀。
